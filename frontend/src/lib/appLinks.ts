/** Public URLs for Gems Assist — Group 3 (NEPTUNE). */
export const APP_LINKS = {
  /** Hosted frontend — works on any device, any network. */
  live: "https://gems-class-flow.base44.app",
  /** Local full stack (website + API). */
  local: "http://localhost:5173",
  github: "https://github.com/FARHANMOHAMMED-R/Gems-Hackathon",
  /** Run at repo root to get a public https link → localhost:5173 */
  shareCommand: "npm run share",
} as const;
